import { NextResponse } from 'next/server';

export function middleware() {
	console.log('🔧 Simple Middleware: Request received');
	return NextResponse.next();
}

export const config = {
	matcher: ['/api/:path*'],
};
