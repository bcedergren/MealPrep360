declare namespace NodeJS {
	interface ErrorConstructor {
		// Using the definition from @types/node
	}

	interface CustomErrorConstructor extends ErrorConstructor {
		// Using the definition from @types/node
	}
}
